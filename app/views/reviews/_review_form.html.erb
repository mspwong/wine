<% form_for([@wine, @review]) do |f| %>
    <%= f.error_messages %>

    <p>
      <%= f.label :reviewer %>*<br/>
      <%= f.text_field :user.name %>
    </p>

    <p>
      <%= f.label :body %><br/>
      <%= f.text_area :body %>
    </p>

    <p>
      <%= f.submit "Save" %>
    </p>
<% end %>